def makeTrainingData(args, x_train, y_train, ):
  if args.oldMethod:
    # Binary method only

  else:
    # Categorical method




for factor in crouchingLayers:
    initVal -= int(factor)
    if initVal < 0:
